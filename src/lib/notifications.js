// Sent on window when notifications are read on the Notifications screen, so the bell
// in the top bar updates straight away instead of at its next check.
export const NOTIFICATIONS_CHANGED = 'cirf:notifications-changed'
